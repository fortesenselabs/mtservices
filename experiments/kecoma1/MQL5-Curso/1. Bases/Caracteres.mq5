
void OnInit() {
   // Char se suele usar para guardar caracteres, pero también se puede usar para guardar números
   char a = "a";  // MAL: Para guardar un único caracter hay que usar las comillas simples '', "" estas comillas se usan para guardar varios carácteres
   char b = 'a';
   
   Print(b);
   
   // Podemos cambiar el valor
   b = 'b';
   
   Print(b);
   
   // Vamos a probar el código en el probador de estrategias
}