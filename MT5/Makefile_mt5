build:
	docker build -t mt5 -f ./Dockerfile.mt5.pywindows .

run: build
	docker run --rm -d -p 8080:8080 --name mt5 -v mt5:/data mt5

shell: 
	docker exec -it mt5 sh

users: build
	docker exec -it mt5 adduser novouser
